"use strict";
let myName = 'Melvin';
let meaningOfLife;
let isLoading;
let album;
let mixed; //can hold either string or number 
myName = 'John';
meaningOfLife = 42;
isLoading = true;
album = 1984;
mixed = "Van Halen";
mixed = 1983;
//by default typescript assigns any type to the variable if the type is not assigned 
const sum = (a, b) => {
    return a + b;
};
let re = / \w + /g;
